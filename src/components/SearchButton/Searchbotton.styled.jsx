import styled from '@emotion/styled';

export const SearchBtn = styled.button`
   border-radius: 50%;
   padding: 6px;
   &:hover,
   &:focus{
       background-color: red;
       outline: none;
       border-color: red;
       border: 2px;
   }
`;