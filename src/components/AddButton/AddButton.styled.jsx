import styled from '@emotion/styled';

export const AddBtn = styled.button`
  border-radius: 50%;
   padding: 8px;
   margin-top: 3px;
   margin-bottom: 3px;

   &:hover,
   &:focus{
       background-color: red;
       outline: none;
       border-color: red;
       border: 2px;
   }
`;
